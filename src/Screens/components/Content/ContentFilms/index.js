import React from "react";
import { FlatList, Image, StyleSheet, Text, View } from "react-native";


const ContentFilms = (props) => {
    const { content } = props;

    
}

export default ContentFilms;

const styles = StyleSheet.create({
    
});